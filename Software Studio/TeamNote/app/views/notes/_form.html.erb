<%= form_for(@note) do |f| %>
  <% if @note.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@note.errors.count, "error") %> prohibited this note from being saved:</h2>

      <ul>
      <% @note.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :subject %><br>
    <%= f.text_field :subject %>
  </div>
  <div class="field">
    <%= f.label :content %><br>
    <%= f.text_area :content %>
  </div>
  <br />
  
  <p> Add User Permissions: </p>
  <%= f.fields_for :alcs do |builder| %>
    <%= render 'alc_fields', :f => builder %>
  <% end %>

  <br />
  <br /> 

  <div class="actions">
    <%= f.submit %>
  </div>

  <br />

  

<% end %>
